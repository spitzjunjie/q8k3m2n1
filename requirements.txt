# =====================================================================
# 补全 + 加上上界
# =====================================================================
# 原版缺了 6 个在代码里 import 了、但没声明的包：
#   efinance / groq / huggingface_hub / openai / tushare / zhipuai
# 缺了它们，CI 里那些模块只会静默走 except 分支，功能悄悄失效。
#
# 另外原版全是 >=，没有上界。akshare 是个更新非常频繁、
# 经常改接口的库，不锁上界等于把生产流水线的稳定性交给上游。
# 生产环境建议进一步用 pip-compile 生成带哈希的 lock 文件。
# =====================================================================

# ---- 行情数据 ----
akshare>=1.12.0,<2.0.0
baostock>=0.9.3,<2.0.0
tushare>=1.4.0,<2.0.0
efinance>=0.5.0,<1.0.0

# ---- 数据处理 ----
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scipy>=1.10.0,<2.0.0          # metrics.py 的 t 分布用；没有会退化成正态近似

# ---- 机器学习 ----
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<4.0.0
joblib>=1.3.0,<2.0.0

# ---- LLM / NLP ----
huggingface_hub>=0.20.0,<1.0.0
groq>=0.9.0,<1.0.0
openai>=1.30.0,<2.0.0
zhipuai>=2.0.0,<3.0.0

# ---- 网络 / 解析 ----
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0

# ---- 配置 ----
python-dotenv>=1.0.0,<2.0.0   # 自动加载 .env，见密钥配置指南第 5 步
