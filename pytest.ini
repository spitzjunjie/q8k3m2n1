[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    network: 需要联网的测试（CI 里可以 -m "not network" 跳过）
    slow: 慢测试
