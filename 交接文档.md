# 项目交接文档：股票量化策略回测系统

> 最后更新：2026-07-28

## 项目概述

这是一个**多策略股票量化回测系统**，运行在 GitHub Actions 上自动执行股票交易策略回测。

- **GitHub 仓库**：`spitzjunjie/q8k3m2n1`
- **Pages 部署**：`https://spitzjunjie.github.io/q8k3m2n1/`
- **本地代码目录**：`c:\Users\xrs08\Desktop\腾讯openclaw\stock_intelligence\multi_strategy_trading`

---

## 当前状态

| 项目 | 状态 | 说明 |
|------|------|------|
| 代码修复 | ✅ 完成 | 修复了 8 个 bug |
| 回测数据 | ✅ 成功 | 7/10-7/24 已回测，产生 145 笔交易 |
| 自动修复 CI/CD | ✅ 完成 | auto-fix.yml v2 已部署 |
| GitHub Pages | ✅ 同步 | 数据已更新 |

---

## 核心 Workflow

### 1. 历史数据补齐 (`history_catchup.yml`)

**用途**：补齐指定日期范围的历史回测数据

**参数**：
- `start_date`：回测开始日期（YYYYMMDD，如 20260710）
- `end_date`：回测结束日期（YYYYMMDD，如 20260726）
- `days`：回测天数（与 start_date/end_date 互斥）
- `source`：数据源（tushare/akshare）

**触发命令**：
```bash
# 指定日期范围
gh workflow run history_catchup.yml --field start_date=20260710 --field end_date=20260726 -R spitzjunjie/q8k3m2n1

# 指定天数（从今天往回数）
gh workflow run history_catchup.yml --field days=17 -R spitzjunjie/q8k3m2n1
```

**最新成功 Run**：#30350704406（1h44m，2026-07-28）

### 2. 每日单次回测 (`backtest.yml`)

**用途**：每天收盘后自动运行一次回测，生成推荐股票

**触发时间**：周一到周五 UTC 10:30（中国时间 18:30）

### 3. 自动修复 (`auto-fix.yml`)

**用途**：当 backtest 或 history_catchup 失败时，自动检测并修复常见 bug

**自动修复的问题**：
1. `has_signal` bug（`\\n` 字面字符串问题）
2. 日期比较逻辑错误（缺少 `<= today_str`）
3. `tushare_helper` 硬编码起始日期
4. `history_catchup` push 前未 pull

---

## 已修复的 Bug（2026-07-28）

| # | 文件 | 问题 | 修复 | Commit |
|---|------|------|------|--------|
| 1 | `trading/simulator.py` | `\\n` 字面字符串导致 `has_signal` 未定义，被当成注释 | 改为真正换行符 | `02a6af9` |
| 2 | `backtest_history.py` | `d >= benchmark_str` 永远为真（因为今天是最大值） | 改为 `d >= benchmark_str and d <= today_str` | `cb57cac` |
| 3 | `backtest_history.py` | `BENCHMARK_START_DATE` 硬编码 `datetime.now()` 无法指定历史日期 | 新增 `calculate_benchmark_start_date()` 动态计算 | `cb57cac` |
| 4 | `tushare_helper.py` | `get_trade_dates` 默认从 `20260526` 开始取（硬编码） | 改为从2年前开始取最近 N 个 | `cb57cac` |
| 5 | `backtest_history.py` | 只支持 `--days` 参数，无法指定精确日期范围 | 新增 `--start-date` 和 `--end-date` | `cb57cac` |
| 6 | `backtest_history.py` | 指定 `start_date` 时 `get_trade_dates(days=days)` 只取少量日期导致过滤失效 | 当指定 start_date 时，先取1000个再过滤 | `b8c9bf4` |
| 7 | `history_catchup.yml` | push 前没有 pull，导致 `rejected` 失败 | 改用 `git stash -u + rebase` 解决并发 | `211b5bd` |
| 8 | `.github/workflows/auto-fix.yml` | 失败后无自动修复机制 | 新增 auto-fix.yml v2，支持5种自动检测修复 | `03ee4c0` |

---

## 关键文件结构

```
stock_intelligence/multi_strategy_trading/
├── backtest_history.py          # 历史回测引擎（核心）
├── backtest.py                  # 每日回测入口
├── trading/
│   └── simulator.py             # 交易模拟器
├── data/
│   ├── tushare_helper.py        # Tushare 数据源
│   └── akshare_helper.py        # AKShare 数据源
├── output/
│   ├── strategy_data.json       # 策略完整数据（累计）
│   └── new_strategy_results.json # 本次回测结果
└── .github/workflows/
    ├── history_catchup.yml      # 历史数据补齐
    ├── backtest.yml             # 每日回测
    └── auto-fix.yml             # 自动修复
```

---

## 回测数据验证

```python
import json
d = json.load(open('output/strategy_data.json', encoding='utf-8'))
dates = set()
for s in d.get('strategies', []):
    for t in s.get('trades', []):
        dates.add(t.get('buy_date', '')[:10])
sorted_dates = sorted(dates)
print('日期范围:', sorted_dates[0], '至', sorted_dates[-1])
print('总交易日:', len(sorted_dates))
```

---

## 常见问题排查

### 1. Workflow 失败：push rejected

**原因**：远程有新的 commit（其他 workflow 或手动 push），本地未同步

**解决方案**：已修改 workflow 使用 `git stash -u + rebase`，应自动解决

### 2. 回测无新交易

**原因**：策略在指定日期范围内没有产生交易信号（正常现象）

**验证**：检查 `new_strategy_results.json` 的 `buy_date` 字段分布

### 3. 日期过滤不生效

**原因**：`get_trade_dates(days=days)` 取的日期数量不足

**解决方案**：确保 `start_date` 参数传入时，代码先取1000个日期再过滤

### 4. `has_signal is not defined`

**原因**：`trading/simulator.py` 中存在 `\\n` 字面字符串

**解决方案**：检查文件第72行附近，确保是真正的换行符而非 `\\n`

---

## 相关链接

- **GitHub Actions**：https://github.com/spitzjunjie/q8k3m2n1/actions
- **Pages 页面**：https://spitzjunjie.github.io/q8k3m2n1/
- **数据文件**：https://github.com/spitzjunjie/q8k3m2n1/tree/main/output

---

## 本地开发注意事项

- 工作目录路径较长：`c:\Users\xrs08\Desktop\腾讯openclaw`
- 包含中文字符，可能导致某些命令行工具编码问题
- 建议使用 Python 脚本或 GitHub 网页操作
- 网络可能较慢，push/pull 需要耐心等待

---

## 下一步建议

1. **监控**：观察每日 `backtest.yml` 是否稳定运行
2. **策略优化**：分析回测结果，优化策略参数
3. **扩展**：根据需求添加新策略或数据源
4. **自动化**：进一步完善 auto-fix 机制，覆盖更多错误模式
